steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/jetsmx-webhooks', '-f', 'infra/webhooks/Dockerfile', '.']
images:
  - 'gcr.io/$PROJECT_ID/jetsmx-webhooks'

